// Rocket Solutions - site config
// Fill these in with your Supabase project values.
// The anon (public) key is safe to expose in client code by design; it only
// permits what your Row Level Security policies allow (here: insert into
// contact_submissions). Never put the service_role key in here.
window.ROCKET_CONFIG = {
  SUPABASE_URL: "YOUR_SUPABASE_URL",        // e.g. https://xxxxxxxx.supabase.co
  SUPABASE_ANON_KEY: "YOUR_SUPABASE_ANON_KEY"
};
